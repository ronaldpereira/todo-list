'use strict';

var _path = require('path');

var _getFiles = require('./get-files');

// Native
(0, _getFiles.npm)((0, _path.resolve)('../mng-test/files-in-package')).then(function (files) {
  console.log(files);

  (0, _getFiles.npm)((0, _path.resolve)('../mng-test/files-in-package-ignore')).then(function (files2) {
    console.log('ignored: ');
    console.log(files2);
  }).catch(function (err) {
    console.log(err.stack);
  });
}).catch(function (err) {
  console.log(err.stack);
});

// Ours